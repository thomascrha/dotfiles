return {
  "JASONews/glow-hover",
  config = function ()
    require'glow-hover'.setup {
        -- The followings are the default values
        max_width = 80,
        padding = 10,
        border = 'shadow',
        glow_path = 'glow'
    }
  end
}
